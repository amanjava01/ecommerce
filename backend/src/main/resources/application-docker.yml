spring:
  datasource:
    url: jdbc:mysql://mysql:3306/ecommerce?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: Amanrana@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  
  data:
    redis:
      host: redis
      port: 6379

app:
  cors:
    allowed-origins: "*"
  jwt:
    secret: mySecretKey123456789012345678901234567890
    expiration: 86400